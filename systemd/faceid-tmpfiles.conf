# Type  Path              Mode  UID   GID   Age  Argument
# Create /run/faceid directory with world-writable permissions for lock files
# This allows PAM module to create lock files regardless of which user it runs as
d       /run/faceid       1777  root  root  -    -
