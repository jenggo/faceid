[Unit]
Description=FaceID Presence Detection Service
Documentation=https://github.com/jenggo/faceid
After=network.target graphical.target
Wants=graphical.target

[Service]
Type=simple
ExecStart=/usr/bin/faceid-presence -c /etc/faceid/faceid.conf
ExecReload=/bin/kill -HUP $MAINPID
StandardOutput=journal
StandardError=journal
Restart=on-failure
RestartSec=10

# Security hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/log/faceid.log

# Resource limits
# Recognition model is 84MB + detection models ~1MB + runtime overhead
# 256MB provides comfortable headroom for face recognition operations
MemoryMax=256M
CPUQuota=10%

# User and group (run as root to access devices)
User=root
Group=root

# Environment
Environment="DISPLAY=:0"
Environment="XAUTHORITY=/home/%u/.Xauthority"

[Install]
WantedBy=graphical.target
